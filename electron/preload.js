// electron/preload.js
// The React app is a plain web app that talks to the local Express server
// over HTTP — it needs no Node/Electron APIs, so nothing is exposed here.
// contextIsolation stays on and nodeIntegration stays off (see main.js).
